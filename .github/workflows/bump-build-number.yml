name: Bump Build Number

on:
  pull_request:
    types: [closed]
    branches:
      - master

jobs:
  bump-build-number:
    if: github.event.pull_request.merged == true
    uses: salva133/My-Workflows/.github/workflows/bump-build-number.yml@d13105a79ae640613704286529470ca59be68a13
    with:
      pr_number: ${{ github.event.pull_request.number }}
