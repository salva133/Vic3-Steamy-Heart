name: Trim Trailing Whitespace

on:
  pull_request:
    types: [closed]
    branches:
      - master

jobs:
  trim-whitespace:
    if: github.event.pull_request.merged == true
    uses: salva133/My-Workflows/.github/workflows/trim-trailing-whitespace.yml@4420419345e92b199dcf148a8deeb20de565d3aa
