name: Trim Trailing Whitespace

on:
  pull_request:
    types: [closed]
    branches:
      - master

jobs:
  trim-whitespace:
    if: github.event.pull_request.merged == true
    uses: salva133/My-Workflows/.github/workflows/trim-trailing-whitespace.yml@d13105a79ae640613704286529470ca59be68a13
