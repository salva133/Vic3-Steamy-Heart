name: Trim Trailing Whitespace

on:
  pull_request:
    types: [closed]
    branches:
      - master

jobs:
  trim-whitespace:
    if: github.event.pull_request.merged == true
    uses: salva133/My-Workflows/.github/workflows/trim-trailing-whitespace.yml@c1243e8785cbe3442c689ce1131a4a016b880961