name: Block Invalid Release Branches

on:
  pull_request:
    branches:
      - master

jobs:
  block-invalid-release-pr:
    uses: salva133/My-Workflows/.github/workflows/block-invalid-release-branches.yml@4420419345e92b199dcf148a8deeb20de565d3aa
    with:
      head_ref: ${{ github.head_ref }}
