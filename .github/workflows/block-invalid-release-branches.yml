name: Block Invalid Release Branches

on:
  pull_request:
    branches:
      - master

jobs:
  block-invalid-release-pr:
    uses: salva133/My-Workflows/.github/workflows/block-invalid-release-branches.yml@latest
    with:
      head_ref: ${{ github.head_ref }}
